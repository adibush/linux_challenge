#!/bin/bash
#add fix to exercise2 here
# You need to delete the line in the host settings file that indicates that every request to the address www.ascii-art.de will go to my IP
#    sudo nano /etc/hosts -->and delete the line
